import datetime

class Human:
    def __init__(self, name, surname, date_of_birth, death_of_death, sex):
        self.name = name
        self.surname = surname
        self.date_of_birth = date_of_birth
        if not death_of_death == 0:
            self.death_of_death = death_of_death

